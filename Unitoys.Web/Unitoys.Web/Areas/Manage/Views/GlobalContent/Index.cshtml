@{
    ViewBag.Title = "全局内容管理";
    Layout = "../Shared/_Layout.cshtml";
}
<script src="~/Content/js/ckeditor/ckeditor.js"></script>
<link rel="stylesheet" type="text/css" href="~/Content/js/uploadify/uploadify.css">
<div class="easyui-layout" fit="true">
    <div data-options="region:'north',border:false" style="height:90px">
        <div id="p" class="easyui-panel" title="高级查询" style="width:100%;height:90px;padding:10px;">
            <form id="ff" method="post">
                <span>名称:</span>
                <input id="name" type="text" class="easyui-textbox" style="height:25px;" />
                <span>类型:</span>
                <select id="globalContentType" class="easyui-combobox" name="globalContentType" style="width:200px;">
                    <option value="">-- 请选择 --</option>
                    <option value="0">招聘</option>
                    <option value="1">售后政策</option>
                    <option value="2">售后渠道</option>
                    <option value="3">常见问题</option>
                    <option value="4">企业简介</option>
                    <option value="6">企业文化</option>
                    <option value="5">商务合作</option>

                </select>
                <span>创建时间:</span>
                <input id="createStartDate" type="text" class="easyui-datetimebox" name="createStartDate" data-options="showSeconds:false" style="height:25px;" />
                <span>-</span>
                <input id="createEndDate" type="text" class="easyui-datetimebox" name="createEndDate" data-options="showSeconds:false" style="height:25px;" />
                <a href="#" class="easyui-linkbutton " data-options="iconCls:'icon-search'" onclick="doSearch()">查询</a>
            </form>
        </div>
    </div>
    <div data-options="region:'center',border:false">
        <table id="grid" class="easyui-datagrid" title="手环设备列表管理" style="width:100%;height:100%"
               data-options="rownumbers:true,fitColumns:true,singleSelect:true,url:'/Manage/GlobalContent/GetList',method:'get',toolbar:toolbar,pagination:true,onDblClickRow:onDbOpenDialog">
            <thead>
                <tr>
                    <th data-options="field:'Name',width:150">名称</th>
                    <th data-options="field:'GlobalContentType',width:120,formatter:formatGlobalContentType">类型</th>
                    <th data-options="field:'CreateDate',width:100,formatter:formatDateTimeInt">创建时间</th>
                </tr>
            </thead>
        </table>
    </div>
</div>
<div id="dlg-edit" class="easyui-dialog" title="添加内容管理" style="width:800px;height:500px;padding:20px"
     data-options="
                    iconCls: 'icon-save',
                    closed:true,
                    buttons: [{
                        text:'保存',
                        iconCls:'icon-ok',
                        handler:saveData
                    }]
                ">
    <form method="post" id="Form" data-options="novalidate:true">
        <input type="hidden" value="" id="ID" name="ID" />
        <table cellpadding="5" class="tb_form">
            <tr>
                <td style="text-align:right" width="100">
                    名称：
                </td>
                <td>
                    <input type="text" name="Name" id="Name" data-options="required:true" class="easyui-textbox" style="width: 220px;height:33px;" maxlength="50" /> &nbsp;
                </td>
            </tr>
            <tr>
                <td style="text-align:right">
                    类型：
                </td>
                <td>
                    <select id="GlobalContentType" name="GlobalContentType" class="easyui-combobox" style="width: 220px; ">
                        <option value="0" selected="selected">招聘</option>
                        <option value="1">售后政策</option>
                        <option value="2">售后渠道</option>
                        <option value="3">常见问题</option>
                        <option value="4">企业简介</option>
                        <option value="6">企业文化</option>
                        <option value="5">商务合作</option>
                    </select>
                </td>
            </tr>
            <tr>
                <td style="text-align:right">
                    内容：
                </td>
                <td>
                    <textarea name="Content" id="Content" style="width: 320px;"></textarea>
                </td>
            </tr>
        </table>
    </form>
</div>
<script type="text/javascript">
    var grid;
    var dlg_Edit;
    var dlg_Edit_form;

    var toolbar = [{
        text: '添加',
        iconCls: 'icon-add',
        handler: function () {
            CKEDITOR.instances["Content"].setData('');
            dlg_Edit_form.form('clear');
            dlg_Edit.dialog('open');
            dlg_Edit.dialog('setTitle', '添加GlobalContent');
            dlg_Edit_form.url = '/Manage/GlobalContent/Add/';
        }
    }, {
        text: '删除',
        iconCls: 'icon-cut',
        handler: del
    }
    ];
    $(function () {
        grid = $("#grid").datagrid();
        dlg_Edit = $("#dlg-edit").dialog();
        dlg_Edit_form = dlg_Edit.find('form');

        CKEDITOR.replace('Content', {
            htmlEncodeOutput: true,//内容输出时编码
            allowedContent: true,//关闭标签过滤
            fullPage: false,//
            image_previewText: '',//预览区域显示内容
            filebrowserImageUploadUrl: "/Manage/PageShow/UploadImgAsync"
        });
    })
    function onDbOpenDialog(index, rowData, value) {
        dlg_Edit.dialog('open');
        dlg_Edit_form.form('clear'); //清除之前面选中的表单
        dlg_Edit.dialog('setTitle', '您正在查看的是：' + rowData.Title + " 的信息");
        dlg_Edit_form.form('load', rowData); //加载到表单的控件上

        dlg_Edit_form.url = '/Manage/GlobalContent/Update';

        CKEDITOR.instances['Content'].setData($('#Content').val());
    }


    function formatGlobalContentType(value, rec, index) {
        switch (value) {
            case 0:
                return '招聘';
            case 1:
                return '售后政策';
            case 2:
                return '售后渠道';
            case 3:
                return '常见问题';
            case 4:
                return '企业简介';
            case 5:
                return '商务合作';
            case 6:
                return '企业文化';
            default:
                break;
        }
        return value;
    }

    function formatUrl(value, rec, index) {
        if (value != "") {
            return "<a href='" + value + "' target='_blank' style='color:#7f99be'>查看" + value + "</a>";
        }
        return value;
    }

    //保存数据
    function saveData() {
        dlg_Edit_form.form('submit', {
            url: dlg_Edit_form.url,
            onSubmit: function () {
                return $(this).form('validate');
            },
            success: successCallback
        });
    }

    function doSearch() {
        $("#grid").datagrid('load', {
            name: $("#name").val() == null ? "" : $("#name").val(),
            globalContentType: $("#globalContentType").combobox("getValue"),
            createStartDate: $("input[name$='createStartDate']").val(),
            createEndDate: $("input[name$='createEndDate']").val(),
        });
    }

    //删除数据
    function del() {
        var arr = getSelectedArr();
        if (arr.length > 0) {
            $.messager.confirm('提示信息', '您确认要删除选中的记录吗?', function (data) {
                if (data) {
                    $.ajax({
                        url: '/Manage/GlobalContent/Delete',
                        type: 'post',
                        data: "ID=" + getSelectedID(),
                        error: function () {
                            Msgalert('错误', '删除失败!', 'error');
                            grid.datagrid('clearSelections');
                        },
                        success: function (data) {
                            if (data.Success) {
                                Msgfade(arr.length + "条记录" + data.Msg); //提示消息
                                grid.datagrid('reload');
                                grid.datagrid('clearSelections'); //清除所有选中的元素
                            } else {
                                Msgalert('错误', data.Msg, 'error');
                            }
                        }
                    });
                }
            });
        } else {
            Msgshow('请先选择要删除的记录。');
        }
    }
</script>
